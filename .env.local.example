NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

# Server-side only — created in the Moove dashboard under Manage > API Keys.
# Needs the payment_link:create scope (and payment_link:read to list links).
# Shown once at creation, so copy it immediately. Never expose this to the browser.
MOOVE_API_KEY=
